{% extends 'base.html.twig' %}

{% block content %}
    <section>
        <h3>add feedback...</h3>
        {{ form_start(form) }}
        {{ form_row(form.author, {'label': 'Author:'}) }}
        {{ form_row(form.text, {'label': 'Feedback:'}) }}
        {{ form_row(form.save, {'label': 'Submit', 'attr': {'id': 'submitBtn'}}) }}
        {{ form_end(form) }}

    </section>
{% endblock %}